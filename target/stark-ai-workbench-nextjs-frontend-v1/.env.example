# =============================================================================
# Environment Variables — Template
# =============================================================================
# To set up a new local environment:
#   1. Copy this file to .env.local       (cp .env.example .env.local)
#   2. Fill in the values below
#   3. .env.local is gitignored and never committed
#
# Get Supabase credentials from:
#   https://supabase.com/dashboard/project/_/settings/api
# =============================================================================


# Supabase project URL (browser-exposed)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Publishable key (browser-exposed; respects RLS).
# Used by createClient() and middleware.
# This is what Supabase used to call the "anon key".
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Secret key (server-side ONLY; bypasses ALL RLS).
# Used by createAdminClient() for portal admin operations.
# This is what Supabase used to call the "service_role key".
# Never expose to the browser. Never commit. Never log.
SUPABASE_SECRET_KEY=sb_secret_...

# Public site URL — used for auth redirects, email confirmation links, etc.
# Dev:        http://localhost:3000
# Staging:    https://your-staging.vercel.app
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000


# -----------------------------------------------------------------------------
# ADK Bundle (BIM-002 — native connector; the Python wrapper is retired)
# -----------------------------------------------------------------------------

# Chat service mode: 'mock' | 'live'. Anything else (or unset) falls back to
# mock — the app must never accidentally ship live-wired.
NEXT_PUBLIC_CHAT_MODE=mock

# Per-bundle api_server base URLs (server-side ONLY — no NEXT_PUBLIC prefix;
# never reach the client bundle). One var per bundle declared in
# config/agents.manifest.json (the manifest carries these NAMES, never URLs).
# Real values go in .env.local only — never commit.
ADK_BUNDLE_URL_V1=https://<bundle-service>-<project-number>.<region>.run.app
ADK_BUNDLE_URL_V2_LOCAL=http://127.0.0.1:8000


# -----------------------------------------------------------------------------
# GCS Instructions (BIM-005 — Mission Control LIVE)
# -----------------------------------------------------------------------------

# Bucket + base folder holding per-agent instruction objects (server-side
# ONLY). Object layout: {GCS_BASE_FOLDER}/{agent}/{agent}_instructions.txt
# Credentials: NONE here by design — ADC is used (`gcloud auth
# application-default login` locally; the attached service account deployed).
GCS_BUCKET=your-instructions-bucket
GCS_BASE_FOLDER=agent-instructions
