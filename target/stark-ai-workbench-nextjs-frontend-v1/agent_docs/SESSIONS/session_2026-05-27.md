# Session: 2026-05-27

Factory Run 001 — Cyberize Agentic Automation Next.js conversion. Day 1.

## What was completed

- **Phase 0 Discovery** — read all factory + project docs, surfaced 4 conflicts (Jest vs Vitest, Next 16 vs 15, /auth vs /login, service return shape), resolved with operator
- **Skill install (mid-Phase 0 interrupt)** — installed `frontend-design`, `skill-creator`, `stark-frontend-first` into `.claude/skills/`
- **Phase 0.5 Cleanup** — removed 15 QR-residue targets (routes, components, services, stores, types, utils, orphan layout). Renamed `package.json` to `adk-agent-harness-nextjs-frontend-v1`. Added `agent_docs/**` to tsconfig exclude. Created `.env.example` with the kit's 4 real env vars
- **Phase 1 Types** — created `src/types/index.ts` with all 15 DATA_CONTRACT §1 types
- **Phase 2 Services (revised)** — created `chatService`, `profileService`, `instructionsService` + barrel + 6 contract tests. **Dropped `authService.ts`** entirely per Lesson 2
- **Phase 3 Mock Data** — created realistic per-agent instructions, seeded message sessions (incl. canonical `ghl_mcp_agent` markdown table), sample profile store, deterministic response generator. Wired services to mocks

## Key decisions

| Decision | Choice | Why |
|---|---|---|
| Test framework | Jest (not Vitest) | Kit has 81 tests passing on Jest |
| Next.js version | Stay on 16 | Kit baseline; module doctrine doc was stale |
| Login route | `/auth` reskinned | Don't fork kit's wired route |
| Service return shape | Direct (DATA_CONTRACT) | Mock-to-real swap stays clean |
| Store directory | `src/store/` singular | Kit convention |
| Cyberize routing | New `(cyberize)` route group, nested admin gate for mission-control | Matches kit's "route group = role boundary" pattern |
| Profile persistence | In-memory only | Phase 5 chat store can add localStorage if needed |
| `authService` | NOT authored | Kit's auth is complete (Lesson 2) |

## Mistakes + lessons captured

- **`proxy.ts` → `middleware.ts` rename misdiagnosis** — reverted mid-Phase 0.5. Next.js 16 renamed convention; kit was right. → **Lesson 1** captured in `STARTER_KIT_FEEDBACK.md` + memory
- **Almost wrote `authService.ts` as a redundant wrapper** — operator pushed back. → **Lesson 2** captured

## Verification

- 87/87 tests passing (81 kit + 6 service contract) at end of day
- `npx tsc --noEmit` clean
- `npm run build` clean (after operator dropped `.env.local`)

## Pending for next session

- Phase 4 approval (Login Screen)

## Closing state

`RECOVERY.md` updated. `STARTER_KIT_FEEDBACK.md` has 8 kit findings + 2 agent lessons documented. Memory has 2 feedback entries indexed in `MEMORY.md`.
